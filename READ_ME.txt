// Import the database to the server "test_blog.sql" ;

//go to the database 'a_login' table and insert data
//'a_fname'=(your full name) ;

//'a_uname'=(your new username)(which you have to memorise) ;
//'a_pass'=(your new password)(which you have to memorise);

// Run the main file which is  "index.php" ;


password is 'aunsu' ;